<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zmkj.mapper.mysql.OptAssistDecsoluDao">
    <select id="searchByStatus" parameterType="string">
        select * from opt_assist_decsolu  where status = #{status}
    </select>

</mapper>